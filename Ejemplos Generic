---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Link
https://desarrolloweb.com/articulos/generics-typescript.html
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

//el parámetro será un array de elementos del tipo genérico.

function display<T>(valor: T[]): T[] {
  console.log(valor.length);
  return valor;
}
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
//Dos genéricos en una cabecera de función.

function prueba<T, K extends keyof T>(obj: T, key: K) {
  return obj[key];
}
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
function display(valor: number): number {
  console.log(valor);
  return valor;
}

function displayString(valor: string): string {
  console.log(valor);
  return valor;
}
